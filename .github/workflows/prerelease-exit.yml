# Generated by smthrs from //.github:github. Do not edit; run: smthrs //.github:github --write
name: prerelease-exit
on:
  workflow_dispatch:
    inputs:
      branch:
        description: Exit prerelease mode on release branch
        required: true
        default: main
        type: string
permissions:
  contents: write
jobs:
  prerelease-exit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: "0"
      - name: Checkout pinned native sources
        run: node scripts/factory/checkout-native.mjs
      - uses: "./.github/actions/setup"
      - run: pnpm exec smthrs target //:prereleaseExit --write
