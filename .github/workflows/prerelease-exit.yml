# Generated by smthrs from //.github:github. Do not edit; run: smthrs //.github:github --write
name: prerelease-exit
on:
  workflow_dispatch:
    inputs:
      branch:
        description: Exit prerelease mode on release branch
        required: true
        default: main
        type: string
permissions:
  contents: write
jobs:
  prereleaseExit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
        with:
          cache-url: "${{ secrets.SMITHERS_CACHE_URL }}"
          cache-token: "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: pnpm exec smthrs '//:prereleaseExit'
