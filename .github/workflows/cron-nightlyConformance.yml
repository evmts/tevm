# Generated by smthrs from //.github:github. Do not edit; run: smthrs //.github:github --write
name: cron-nightlyConformance
on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
jobs:
  cron-nightlyConformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: "0"
      - name: Checkout pinned native sources
        run: node scripts/factory/checkout-native.mjs
      - uses: "./.github/actions/setup"
      - run: pnpm exec smthrs target //test:nightlyConformance
