# Generated by smthrs from //.github:github. Do not edit; run: smthrs //.github:github --write
name: jsr-publish
on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Run in dry-run mode without publishing
        required: true
        default: false
        type: boolean
permissions:
  contents: read
  idToken: write
jobs:
  tevm-publishJsr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: ./.github/actions/setup
        with:
          cache-url: "${{ secrets.SMITHERS_CACHE_URL }}"
          cache-token: "${{ secrets.SMITHERS_CACHE_TOKEN }}"
      - run: pnpm exec smthrs '//tevm:publishJsr'
