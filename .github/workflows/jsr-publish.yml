# Generated by smthrs from //.github:github. Do not edit; run: smthrs //.github:github --write
name: jsr-publish
on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Run in dry-run mode without publishing
        required: true
        default: false
        type: boolean
permissions:
  contents: read
  id-token: write
jobs:
  jsr-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: "0"
      - name: Checkout pinned native sources
        run: node scripts/factory/checkout-native.mjs
      - uses: "./.github/actions/setup"
      - run: pnpm exec smthrs target //tevm:publishJsr
