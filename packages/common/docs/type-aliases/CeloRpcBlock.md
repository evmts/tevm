[**@tevm/common**](../README.md)

***

[@tevm/common](../globals.md) / CeloRpcBlock

# Type Alias: CeloRpcBlock\<blockTag, includeTransactions\>

> **CeloRpcBlock**\<`blockTag`, `includeTransactions`\> = `RpcBlock`\<`blockTag`, `includeTransactions`, `RpcTransaction`\<`blockTag` *extends* `"pending"` ? `true` : `false`\>\>

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `blockTag` *extends* `BlockTag` | `BlockTag` |
| `includeTransactions` *extends* `boolean` | `boolean` |
