# @tevm/common

Viem chain definitions. `Common` is a viem `Chain`, and `tevmDefault` is the Foundry chain (31337). There is no mutable JavaScript execution configuration or `ethjsCommon` object. Native execution rules belong to ZEVM.
